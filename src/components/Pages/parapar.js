import React, { Component } from 'react';
import { Container, Row, Col, Button, Card, Image, ProgressBar } from 'react-bootstrap'

const ParaPar = ({
}) => (
    <div>
        par a par
    </div>
)

export default ParaPar